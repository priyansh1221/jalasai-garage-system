# New UI Shell

This folder contains the New UI HTML/CSS shell and its assets.

Runtime JavaScript is not mirrored here. `NEW UI/index.html` loads the shared source files from root `../js/`, including the New UI-specific `../js/new-ui.js`.

Do not recreate `NEW UI/js/`. Keep all business/runtime JavaScript changes in root `js/`.
